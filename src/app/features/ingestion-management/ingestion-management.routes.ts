import { Routes } from '@angular/router';
import { IngestionList } from './components/ingestion-list/ingestion-list';

export const routes: Routes = [
    {
        path: '',
        component: IngestionList
      }
];
